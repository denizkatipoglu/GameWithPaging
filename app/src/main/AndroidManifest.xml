<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gamewithpaging">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".GameApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Game">
        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.db.DatabaseActivity" />
        <activity android:name=".view.network.NetworkOnlyActivity" />
        <activity android:name=".view.networkanddb.NetworkAndDatabaseActivity" />

        <activity
            android:name=".view.gamedetail.GameDetailActivity"
            android:theme="@style/NoActionBar"/>
    </application>

</manifest>